# Google Sheets content connection

The site is already connected to the spreadsheet:

Ben Oz Digital Gallery — Content Manager
https://docs.google.com/spreadsheets/d/1qS2N_-BPKIP3zXuTYGSFe0zh18u7ECGdZxDspjJG0Ts/edit

## One-time Google Sheets action

For the deployed site to read the table without login:

1. Open the spreadsheet.
2. Choose **File → Share → Publish to web**.
3. Select **Entire document** and **Comma-separated values (.csv)**, or publish the two tabs Collections and Works.
4. Click **Publish**.

No URL needs to be pasted into the code. The spreadsheet ID and tab names are already configured.

If the sheet is unavailable or unpublished, the site automatically uses the current local data so the gallery never appears empty.
